## global info
```
## Control plane cluster
IP1=${IP1}
IP2=${IP2}
IP3={IP3}
LBIP=${LBIP}
PEERS=(${IP1} ${IP2} ${IP3})
ETCD_ADDR=http://${IP1}:2379,http://${IP2}:2379,http://${IP3}:2379

## docker image
ETCD_IMG=${ETCD_IMG}
KUBE_IME=${KUBE_IMG}

## kube config
ADMIN_KCF={ADMIN_KCF}



```


## admission-control